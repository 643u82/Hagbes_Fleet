<odoo>
    <!-- ✅ Disable Create in Kanban View -->
    <record id="view_hr_job_kanban_hide_create_button" model="ir.ui.view">
        <field name="name">hr.job.kanban.no.create</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr_recruitment.view_hr_job_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//kanban" position="attributes">
                <attribute name="create">false</attribute>
                <attribute name="on_create" remove="1"/>
            </xpath>
        </field>
    </record>

    <!-- ✅ Disable Create in List (Tree) View -->
    <record id="view_hr_job_tree_hide_create_button" model="ir.ui.view">
        <field name="name">hr.job.list.no.create</field>
        <field name="model">hr.job</field>
        <field name="inherit_id" ref="hr_recruitment.hr_job_view_tree_inherit"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="create">false</attribute>
                <attribute name="on_create" remove="1"/>
            </xpath>
            <xpath expr="//field[@name='name']" position="after">
              <field name="company_id"/>
              <field name="analytic_account_id"/>
            </xpath>

        </field>
    </record>
</odoo>




