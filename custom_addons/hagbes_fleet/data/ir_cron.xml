<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- License Expiry Check (Weekly) -->
        <record id="ir_cron_check_license_expiry" model="ir.cron">
            <field name="name">Fleet: Check Driver License Expiry</field>
            <field name="model_id" ref="hr.model_hr_employee"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_license_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">weeks</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Overdue Allocation Return Check (Daily) -->
        <record id="ir_cron_check_overdue_returns" model="ir.cron">
            <field name="name">Fleet: Check Overdue Allocations</field>
            <field name="model_id" ref="model_hagbes_fleet_allocation"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_overdue_returns()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

        <!-- Pending Approval Reminder (Daily) -->
        <record id="ir_cron_approval_reminder" model="ir.cron">
            <field name="name">Fleet: Pending Approval Reminder</field>
            <field name="model_id" ref="model_fleet_requisition"/>
            <field name="state">code</field>
            <field name="code">model._cron_approval_reminder()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
